# Address Validator

A full-stack address validation service with:

- React single-page app served from S3 + CloudFront
- NestJS backend deployed as AWS Lambda behind API Gateway
- Infrastructure defined using AWS CDK
- Monorepo managed with pnpm workspaces

## Repository Structure
